import { AuditoriaInterceptor } from './auditoria.interceptor';

describe('AuditoriaInterceptor', () => {
  it('should be defined', () => {
    expect(new AuditoriaInterceptor()).toBeDefined();
  });
});
